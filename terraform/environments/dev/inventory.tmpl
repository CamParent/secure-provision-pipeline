all:
  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519_ci
  children:
    bastion:
      hosts:
        bastion01:
          ansible_host: ${bastion_ip}
    web:
      hosts:
        web01:
          ansible_host: ${web_ip}
    db:
      hosts:
        db01:
          ansible_host: ${db_ip}
