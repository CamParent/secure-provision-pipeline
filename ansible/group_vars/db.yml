---
# db01 needs its database port open. For now this is allowed from the
# whole lab subnet, same as everything else (we're flat/non-VLAN right
# now - see README "Future work"). Once network segmentation is in place,
# this should be tightened to only the web tier's source IP instead of
# open to the whole subnet.
common_hardening_ufw_allowed_ports:
  - "{{ common_hardening_ssh_port }}"
  - 5432
